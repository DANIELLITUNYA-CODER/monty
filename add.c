#include "monty.h"

void f_add(bus_t *bus)
{
    if (bus->stack != NULL && bus->stack->next != NULL)
    {
        /* Implementation of add */
    }
    else
    {
        /* Handle error: not enough elements in the stack */
    }
}

