#include "monty.h"

void f_pint(bus_t *bus)
{
    if (bus->stack != NULL)
    {
        /* Implementation of pint */
    }
    else
    {
        /* Handle error: trying to pint from an empty stack */
    }
}
